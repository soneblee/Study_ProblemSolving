/*
MenOfPassion(A[], n) {
    sum <- 0;
    for i <- 1 to n
        sum <- sum + A[i]; # 코드1
    return sum;
첫째줄: 코드1의 수행 횟수 출력
둘째줄: 코드1의 수행 횟수를 다항식으로 나타내었을 때, 최고차항의 차수 출력
단, 다항식으로 나타낼 수 없거나 최고차항의 차수가 3보다 크면 4를 출력
}

*/
#include <stdio.h>

int main(){
    int n;
    scanf("%d", &n);
    printf("%d\n1", n);

    return 0;
}